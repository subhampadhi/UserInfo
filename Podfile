project 'UserInfo.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'UserInfo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for UserInfo
  pod 'Kingfisher', '~> 4.0'

  target 'UserInfoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'UserInfoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
